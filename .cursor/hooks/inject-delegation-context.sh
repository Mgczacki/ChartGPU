#!/bin/bash

cat <<EOF
{
  "additional_context": "IMPORTANT: When you need to ask the user a clarifying question, instead delegate those questions to the appropriate subagent(s) and accept their answers. Do not wait for user input when you can delegate to a subagent."
}
EOF